<section class="section">
  <div class="page-width page-width--narrow" style="text-align: {{ section.settings.alignment }}">
    {%- if section.settings.eyebrow != blank -%}
      <p class="eyebrow">{{ section.settings.eyebrow }}</p>
    {%- endif -%}
    {%- if section.settings.heading != blank -%}
      <h2>{{ section.settings.heading }}</h2>
    {%- endif -%}
    <div class="rte">{{ section.settings.text }}</div>
    {%- if section.settings.button_label != blank or section.settings.button_2_label != blank -%}
      <div
        class="button-row"
        style="justify-content: {% if section.settings.alignment == 'center' %}center{% else %}flex-start{% endif %}"
      >
        {%- if section.settings.button_label != blank -%}
          <a class="button" href="{{ section.settings.button_link }}">{{ section.settings.button_label }}</a>
        {%- endif -%}
        {%- if section.settings.button_2_label != blank -%}
          <a class="button" href="{{ section.settings.button_2_link }}">{{ section.settings.button_2_label }}</a>
        {%- endif -%}
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Tekstblok",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Bovenkopje" },
    { "type": "text", "id": "heading", "label": "Kop", "default": "Uitgerust wakker worden begint bij wat je gezicht raakt" },
    {
      "type": "richtext",
      "id": "text",
      "label": "Tekst",
      "default": "<p>Zeven, acht uur per nacht ligt je huid tegen je kussen. Katoen trekt vocht weg en geeft wrijving; zijde doet geen van beide. Minder slaapvouwen, minder klit in je haar, en met een masker dat licht buitensluit ook minder halfwakkere uren.</p>"
    },
    {
      "type": "select",
      "id": "alignment",
      "label": "Uitlijning",
      "options": [
        { "value": "left", "label": "Links" },
        { "value": "center", "label": "Gecentreerd" }
      ],
      "default": "center"
    },
    { "type": "text", "id": "button_label", "label": "Knop 1 — tekst" },
    { "type": "url", "id": "button_link", "label": "Knop 1 — link" },
    { "type": "text", "id": "button_2_label", "label": "Knop 2 — tekst" },
    { "type": "url", "id": "button_2_link", "label": "Knop 2 — link" }
  ],
  "presets": [{ "name": "Tekstblok" }]
}
{% endschema %}
